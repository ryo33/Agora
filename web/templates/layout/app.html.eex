<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A platform for online communities.">
    <meta name="author" content="">

    <title><%= Application.get_env(:agora, :title) %></title>

    <meta property="og:title" content="<%= og(assigns, :title, Application.get_env(:agora, :title)) %>" />
    <meta property="og:type" content="<%= og(assigns, :type, "website") %>" />
    <meta property="og:url" content="<%= og(assigns, :url, "") %>" />
    <meta property="og:description" content="A platform for online communities.">
    <meta property="og:site_name" content="<%= Application.get_env(:agora, :title) %>" />
    <meta property="og:image" content="<%= og(assigns, :image, "") %>" />

    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>
  <script>
    window._source = {
      title: "<%= Application.get_env(:agora, :source)[:title] %>",
      url: "<%= Application.get_env(:agora, :source)[:url] %>"
    }
  </script>

  <body>
    <div id="container"></div>
    <%= render @view_module, @view_template, assigns %>
    <script src="<%= static_path(@conn, "/js/app.js") %>" async></script>
  </body>
</html>
